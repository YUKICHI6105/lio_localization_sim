diff --git a/docs/experiment_history/17_rotation_error_stage_decomposition_20260730.md b/docs/experiment_history/17_rotation_error_stage_decomposition_20260730.md
new file mode 100644
index 0000000..891d7f8
--- /dev/null
+++ b/docs/experiment_history/17_rotation_error_stage_decomposition_20260730.md
@@ -0,0 +1,90 @@
+# 実験履歴: 旋回時過渡誤差の3段分解と原因確定・対策方針 (2026-07-30)
+
+## 目的
+
+符号バグ修正後(16番)も残る「旋回時に位置誤差が一時的に10〜28mmへ上がり、数秒で
+1mm前後へ回復する」過渡誤差について、どの段(②壁マッチング / ③因子グラフ融合 /
+①IMU外挿)が誤差を持っているかをbag再生の3段分解で確定し、新目標
+「どんな動きをしても最大位置誤差5mm以内」への対策方針を決める。
+
+実験はSonnetサブエージェントが実施(bag再生のみ、Unity起動なし、ソース変更なし)。
+使用bag: `/tmp/robocon_angvel_fix_gyrosign_20260730.mcap`(旋回込み約40秒、
+ジャイロ符号修正済み)。`bag_component_isolation.sh --record-stages`で
+`/ground_truth_pose` `/scan_match_result` `/state_estimate` `/odom_fast` を記録し、
+各ストリームを真値と自スタンプで比較した。
+
+## 実験A: 3段誤差分解の結果
+
+run: `docs/stage4_runs/stage_decomp_rotation_20260730/`
+(サニティ: evaluator t=15窓 mean=10.31/max=28.21mm、既知基準と整合)
+
+| stream | 窓 | pos最大[mm] | ピーク時刻[s] | pos平均[mm] | yaw最大[deg] |
+|---|---|---:|---:|---:|---:|
+| ②wall(validのみ) | 旋回[9.5,12] | 2.77 | 11.83 | 0.72 | 0.10 |
+| ②wall(validのみ) | 直後[12,16] | 2.18 | 13.25 | 0.63 | 0.24 |
+| ②wall(validのみ) | 全体[3,38] | **2.77** | — | 0.66 | 0.25 |
+| ③state_estimate | 旋回[9.5,12] | 5.18 | 11.98 | 0.94 | 1.38 |
+| ③state_estimate | 直後[12,16] | **24.91** | 13.40 | 13.24 | **2.72** |
+| ①odom_fast | 直後[12,16] | **28.21** | 12.59 | 13.71 | 2.73 |
+
+補助データ: DCSダウンウェイト 0件 / 壁補正unavailable 0件 / 品質reject 0件
+(run全体)。つまり壁補正は**全スキャンがフル重みで受理されている**。
+
+## 実験B: ①のLPF群遅延アブレーション
+
+lpf_tau 0.005→0.001で全窓とも変化±5%以内(t=15窓 max 27.65→26.38mm)。
+**LPF仮説は棄却**。
+
+## 確定した結論
+
+1. **②(壁マッチング測定)は旋回中・直後も含め最大2.8mm/0.25°でほぼ真値**。
+   ICP側の仮説(旋回時測定バイアス、粗探索不足、ノーツ、deskew)はすべて棄却。
+   測定チェーンは既に5mm目標を満たす品質を持っている。
+2. **誤差は③の融合で生まれる**。毎秒40回、~1mm/~0.1°精度の絶対観測(壁prior)を
+   フル重みで受理しながら、事後推定は25mm/2.7°までズレ、回復に4〜6秒かかる。
+   ①は③の上に~3mmの外挿誤差を足すだけ(28.2 vs 24.9mm)。
+3. メカニズム: **CombinedImuFactor(相対拘束)の剛性が壁prior(絶対拘束)に対して
+   桁違いに高すぎる**。ジャイロノイズ密度はデータシート白色雑音値
+   (4.9e-5 rad/s/√Hz)をそのまま使っており、25ms区間の相対回転σは~8e-6 radと
+   非現実的にタイト。一方壁priorのyaw σは0.3/√N≈0.77°で、実測品質(平均0.05°)
+   より約15倍緩い。高角加速度中にIMUモデル外の誤差(有限差分加速度、代入
+   タイミングのジッタ等)が入ると、グラフは緩い側の変数(バイアス・状態)を曲げて
+   矛盾を吸収し、その汚染が固定ラグ窓の時定数で数秒間抜けない —
+   観測された「旋回直後にピーク、ゆっくり減衰」の署名と一致する。
+4. これはLIO系の既知の失敗様式で、データシート値のIMUノイズは因子グラフでは
+   ほぼ常に楽観的すぎる。LIO-SAM自身の既定config(imuGyrNoise=0.01)は
+   データシート比~200倍に膨らませてある。
+
+## 対策の提案(優先順)
+
+1. **剛性バランスの是正(パラメータのみ、bag再生でA/B検証可能)**
+   - ③のIMUノイズ(gyroscope_sigma / accelerometer_sigma / integration_sigma)を
+     現実的な総合値へ膨張(データシート比10〜50倍から探索)
+   - 壁priorのyaw σを実測品質へ接近(wall_prior_base_yaw_sigma 0.3→0.05程度)
+   - 期待効果: 事後推定の権威が「実測2.8mm/0.25°の壁観測」側へ移り、
+     旋回直後の尾引きが消える。②が2.8mmである以上、**融合の是正だけで
+     5mm目標に届く見込みが高い**
+2. **②の実共分散配信**: planar solverが既に組むGauss-Newton Hessianから
+   H⁻¹ベース(Censi型)の共分散を配信し、固定1e-5・軸別折半・縮退フラグ常時false
+   という人工値を置換する。手調整グローバル値ではなく、スキャンごとの実情報量
+   (旋回中の異方性を含む)が③へ伝わる恒久解
+3. (1)(2)で不足する場合のみ: バイアスのランダムウォークσ・事前分布の見直し
+4. **密結合化(点対線残差をGTSAMカスタム因子として③へ直接入れる、LIO-SAM系譜の
+   構造的終着点)は現時点では不要**と判断。測定が既に2.8mmであり、対策1が
+   パラメータのみで済むため、コスト対効果が合わない
+
+## 方針
+
+- 次の一手は対策1のA/Bスイープ(bag再生のみ、Unity不要)。IMUノイズ膨張率
+  {10x, 30x, 50x} × 壁yaw σ {0.3, 0.1, 0.05} 程度の小グリッドで、旋回窓の
+  max位置誤差が5mm未満になる組を探す
+- 対策2はその結果を見てから着手判断(対策1で5mmに届けば優先度は下がるが、
+  実機移行時のロバスト性のため中期的には実施推奨)
+- 実験生成物: `docs/stage4_runs/stage_decomp_rotation_20260730/`、
+  `docs/stage4_runs/lpf_ablation_tau0p001_20260730/`(いずれも未コミット)
+
+## 対応状態
+
+- 本レポート時点で実装変更なし(分析・方針のみ)
+- ヨー版迷子モード(16番)は既定無効のまま
+- coarse-to-fine平面ソルバー(前セッションで実装済み・効果中立)は無害のため維持
diff --git a/docs/experiment_history/18_sigma_sweep_and_full_run_scan_20260730.md b/docs/experiment_history/18_sigma_sweep_and_full_run_scan_20260730.md
new file mode 100644
index 0000000..bb98ff5
--- /dev/null
+++ b/docs/experiment_history/18_sigma_sweep_and_full_run_scan_20260730.md
@@ -0,0 +1,85 @@
+# 実験履歴: σスイープと全区間旋回走査(2026-07-30、進行中)
+
+17番の方針(IMUノイズ/壁yaw事前分布のσ調整で旋回時過渡誤差を5mm以内へ)の
+検証結果。bag再生のみ、Unity起動なし。
+
+## 1. 使用bagの実態(想定と異なっていた)
+
+`stage_decomp_rotation_20260730/stage_derived`の全区間(約40秒)を走査した結果、
+このbagには**旋回イベントが1回しか含まれていない**ことが判明した。
+
+- t=0〜10.06s: 完全静止
+- t=10.06〜12.8s: 唯一の移動区間(該当の旋回 t≈10.1-11.1s、ヨー変化約-56.2°、
+  峰ヨーレート1.49rad/s)
+- t=12.8〜39.74s: 再び完全静止
+
+周回走行(複数回旋回)を想定していたが、実際は「静止→1回の移動+旋回→静止」の
+単発プロファイルだった。したがって**「毎回の旋回で同程度悪化するか」はこの
+bagだけでは判定できない**(比較対象の旋回が他に無いため)。参考として:
+
+| 区間 | 峰位置誤差[mm] |
+|---|---:|
+| 旋回イベント(t=9.8-14.1窓) | 24.907 @t=13.401s |
+| 静止区間S0(t=25.3-28.6) | 1.332 |
+| 静止区間S1(t=3.3-6.6) | 1.173 |
+
+旋回時は静止時の約19〜21倍。グローバル最大も同じ24.907mm@13.401sで、
+report 17の実測(状態推定24.91mm@13.40s)と完全一致(解析の妥当性を確認)。
+
+## 2. σスイープ結果
+
+baseline(yaw_sigma=0.30, IMU等倍): t=15 mean=10.2/max=27.7mm(report 17記載値)。
+
+| run | wall_prior_base_yaw_sigma | IMU倍率 | t=15 mean/max[mm] | t=20 | t=25 | t=30 |
+|---|---:|---:|---:|---:|---:|---:|
+| sweep_yawsigma_015 | 0.15 | 1x | 11.22/30.48 | 3.88/8.57 | 1.34/1.82 | 0.77/1.36 |
+| sweep_yawsigma_008 | 0.08 | 1x | 11.20/30.56 | 3.93/8.59 | 1.34/1.82 | 0.77/1.36 |
+| sweep_yawsigma_005 | 0.05 | 1x | 11.27/30.60 | 3.90/8.55 | 1.34/1.81 | 0.76/1.36 |
+| sweep_imuinflate_10x | (best上記) | **10x** | **4.04/18.29** | 0.61/1.63 | 0.41/0.93 | 0.28/0.80 |
+
+### 所見(想定と逆だった点)
+
+**壁yaw事前分布を締める(0.30→0.05)だけでは改善せず、むしろ悪化した**
+(t=15 max 27.7→30.5mm、0.15/0.08/0.05の3値でほぼ差が無い)。壁補正の重みを
+機械的に強めても、旋回中に壁観測自体に乗る短時間のノイズ(平均では小さくても
+瞬間的にはある)をより強く状態へ注入するだけで、逆効果になったと考えられる。
+
+**IMUノイズを膨らませる(10倍)方が明確に効いた**。t=15 maxが27.7→18.3mmへ
+34%減。さらに旋回と無関係なt=20〜30の定常区間も軒並み改善しており
+(t=30 max 1.45→0.80mm)、IMU拘束を緩めることが旋回時だけでなく全体の
+精度にもプラスに働くことを示している。これは「IMU相対拘束が壁の絶対拘束
+より硬すぎて、旋回中の予測を過信し、fixed-lag窓の時定数でしか解消しない」
+という17番の機序仮説と整合する。
+
+### 続き(30x/50x、完了)
+
+| run | 倍率 | t=15 mean/max[mm] | t=20 | t=25 | t=30 | t=35 |
+|---|---:|---:|---:|---:|---:|---:|
+| sweep_imuinflate_30x | 30x | 1.32/**12.06** | 0.26/0.60 | 0.22/0.68 | 0.22/0.65 | 0.21/0.64 |
+| sweep_imuinflate_50x | 50x | 0.74/**11.95** | 0.27/0.60 | 0.23/0.86 | 0.25/0.77 | 0.23/0.65 |
+
+10x→30xでt=15 maxが18.3→12.1mmへさらに改善するが、30x→50xでは12.06→11.95mm
+とほぼ横ばい(収穫逓減、頭打ち)。定常区間(t=20〜35)はいずれの倍率でも
+baselineより明確に改善しており、悪化は一切無い。
+
+## 3. 結論と採用パラメータ
+
+**`wall_prior_base_yaw_sigma=0.15` + IMUノイズ(gyroscope/accelerometer/
+integration sigma)×30倍**を採用する。50倍はt=15 maxがほぼ同値(誤差範囲内)
+で定常窓がわずかに悪化するため、より小さい摂動で同等効果の30倍を選ぶ。
+
+| 指標 | baseline | 採用(yaw_sigma=0.15, IMU×30) |
+|---|---:|---:|
+| t=15 max(旋回時) | 27.7〜28.2mm | **12.06mm**(57%減) |
+| t=30 max(定常) | 1.45mm | 0.65mm(改善) |
+
+**5mm目標には未達**(旋回時maxの下限は30x/50xとも約12mmで頭打ち)。計画通り
+ここで本方向のσ調整は打ち切る。
+
+## 対応状態
+
+- コード変更なし。パラメータ調整の効果測定のみ(未適用、yamlへの反映は未実施)
+- `wall_prior_base_yaw_sigma`単独調整は効果薄と確認、以後の候補から除外
+- IMUノイズ膨張は有効だが単独では5mm目標に届かず頭打ち。次の焦点は
+  17番が提案した対策2(②のGauss-Newton Hessianベース実共分散配信、
+  固定1e-5・軸別機械的折半・縮退フラグ常時falseの解消)
diff --git a/docs/experiment_history/19_rotation_error_root_cause_summary_20260730.md b/docs/experiment_history/19_rotation_error_root_cause_summary_20260730.md
new file mode 100644
index 0000000..5c961d1
--- /dev/null
+++ b/docs/experiment_history/19_rotation_error_root_cause_summary_20260730.md
@@ -0,0 +1,84 @@
+# 実験履歴: 旋回時過渡誤差の原因整理(2026-07-30時点)
+
+15〜18番の結果を1枚に整理する。個々の実験詳細は各番号を参照。
+
+## 確定した原因(証拠あり)
+
+### A. 3つのバグ(修正済み、16番)
+
+実旋回を初めて有効化した際の壊滅的破綻(最大159m、ヨー誤差180°)の原因。
+いずれも「yaw=0では無症状」なため、この検証まで一度も発現していなかった。
+
+1. `/odom_fast`のtwistがワールド座標系のまま配信(ボディ座標系であるべき)
+2. `body.MoveRotation()`が非kinematic Rigidbodyの`angularVelocity`を更新しない
+3. **(真因)** ジャイロが擬ベクトルとして正しく変換されていない
+   (`UnityVectorToRos`の鏡映変換に符号反転が抜けていた)
+
+3つとも修正済み。修正後、壊滅的破綻(541/541点非対応等)は再現しなくなった。
+
+### B. 残る過渡誤差は②ではなく③で生まれる(17番)
+
+3段分解(②wall / ③state_estimate / ①odom_fast、真値との突き合わせ)の結果:
+
+- ②(壁マッチング測定)は旋回中も含め**最大2.8mm/0.25°**とほぼ真値
+- ③(因子グラフ融合)で最大24.9mm、①はその上に外挿誤差を足して最大28.2mm
+- この間、DCSダウンウェイト0件・壁補正拒否0件・unavailable 0件
+  (壁補正は**毎回フル重みで受理**されている)
+
+→ 誤差は測定(②)の質ではなく、③の融合の仕方で生まれている。
+
+### C. 融合側の機序: IMU拘束が壁の絶対拘束より硬すぎる(17・18番)
+
+18番のσスイープで機序を実証した。
+
+| 操作 | t=15旋回窓 max[mm] | 判定 |
+|---|---:|---|
+| baseline | 27.7〜28.2 | — |
+| 壁yaw事前分布を締める(0.30→0.05) | 30.5〜30.6 | **悪化**(効果なし) |
+| IMUノイズ×10 | 18.3 | 改善 |
+| IMUノイズ×30 | **12.1** | 改善(最良) |
+| IMUノイズ×50 | 12.0 | 頭打ち(30xと同値) |
+
+「壁を締めても悪化し、IMUを緩めると改善する」という非対称な結果は、
+**問題が"壁を信じていないこと"ではなく"IMU(CombinedImuFactor)の相対拘束が
+硬すぎて、旋回中の予測を過信すること"**であることを直接裏付ける。データ
+シート値のIMUノイズは無回転時は問題にならないが、旋回中の未モデル化誤差
+(有限差分加速度・タイミングジッタ等)を吸収する余地が無い。
+
+## まだ特定できていない点
+
+**IMUノイズをいくら緩めても12mm前後で頭打ちになり、5mm目標に届かない。**
+つまりIMU拘束の硬さは主要因の一つだが、それだけでは説明しきれない
+「もう1つの下限要因」が別に存在する。候補(いずれも未検証):
+
+1. **バイアスのランダムウォークσ**(`bias_acc_random_walk_sigma`/
+   `bias_gyro_random_walk_sigma`)は今回のスイープで触っていない。旋回中に
+   一時的に誤差を吸収したバイアスが、真値へ戻るのに数秒かかっている
+   (=バイアス自身の遅い時定数)可能性がある。18番でスイープしたのは
+   加速度計・ジャイロ・積分の「測定」σのみ。
+2. **②の共分散が固定値・ヒューリスティックであること**(アーキテクチャ文書
+   で以前から指摘済み、17番の対策2)。壁位置の残差は小さくても、旋回中は
+   見える壁面の法線方向の分布が変わり、**ヨー方向の真の情報量(観測可能性)
+   だけが一時的に落ちている**可能性がある。現状の共分散は対応点数のみに
+   基づく(固定1e-5、軸別は機械的折半)ため、この種の幾何学的な劣化を
+   反映できない。実際のGauss-Newton Hessianから共分散を出せば、旋回中に
+   σが自動的に(必要な分だけ)緩み、③が正しく重みを調整できる可能性がある。
+3. ISAM2の`relinearize_threshold_`が、旋回による大きな非線形性の下で
+   最適化の質を落としている可能性(未検証)。
+
+## 追加検証: バイアスランダムウォークσ(候補1)は除外
+
+`bias_acc_random_walk_sigma`/`bias_gyro_random_walk_sigma`を既定値の0.1倍〜100倍
+(1000倍のレンジ)で振っても、t=15旋回窓のmax誤差は12.06〜12.13mmで**完全に
+不変**だった(mean誤差はk=100で1.32→0.57mmまで下がるが、maxには効かない)。
+候補1は12mmの下限要因ではないと確定。次の焦点は候補2(②の実共分散配信)に
+絞られた。
+
+## 現時点の結論
+
+- 破綻(バグ)と過渡誤差(残差)は別問題。バグは解消済み
+- 過渡誤差の所在(③の融合)と機序の方向性(IMU拘束過硬)は実証済み
+- ただしIMU側のパラメータ調整だけでは5mm目標の下限(~12mm)を越えられない
+- 次に切り分けるべきは上記候補1(バイアスσ)と候補2(②の実共分散)のどちらが
+  12mmの下限を作っているか。候補2は17番で「大改造は不要」と判断していたが、
+  今回IMU調整だけでは頭打ちになったことを踏まえ、再評価が必要かもしれない
diff --git a/docs/experiment_history/20_planar_real_covariance_implementation_20260730.md b/docs/experiment_history/20_planar_real_covariance_implementation_20260730.md
new file mode 100644
index 0000000..47b35b3
--- /dev/null
+++ b/docs/experiment_history/20_planar_real_covariance_implementation_20260730.md
@@ -0,0 +1,147 @@
+# 実験履歴: ②の実共分散(Censi型)実装と検証 (2026-07-30)
+
+19番で候補1(バイアスσ)を除外した後の、候補2(②の実共分散配信)の実装結果。
+
+## 実装内容
+
+`lio_localization/src/laser_scan_matching_node.cpp`の平面ソルバー
+(`solve_planar`/`solve_planar_ray_to_wall`)で、最終反復のGauss-Newton情報行列
+`h`(x,y,yaw順、Huber重み込み、正則化前)を新しい外側変数`planar_h`として捕捉し、
+
+```
+Cov(x,y,yaw) = wall_cov_range_sigma_^2 * planar_h^-1
+```
+
+をbackendが期待する(yaw,x,y)対角順に並べ替えて`result.wall.covariance`へ配信する
+ように変更した。固有値(縮退判定)が1e-6以下ならconvariance_validをfalseにし、
+従来のヒューリスティックのみへフォールバックする。固定値`{1e-5,0,0,...}`だった
+箇所を置換。backend側(`wall_use_icp_covariance_`)は既存のクランプ(ヒューリス
+ティックσを下限、縮退σを上限として緩める方向にのみ反映)をそのまま使うため、
+backend側の変更は不要だった。ビルド成功。
+
+## 検証(bag再生、`/tmp/robocon_angvel_fix_gyrosign_20260730.mcap`)
+
+realtime_evaluator_nodeの5秒窓ログ(`t=15.0s recent 5s`が旋回直後窓に相当、
+以前のレポートと同じ定義)で比較。
+
+| 条件 | t=15 mean[mm] | t=15 max[mm] |
+|---|---:|---:|
+| 旧baseline(実共分散なし、17/18番) | 10.2 | 27.7〜28.2 |
+| **実共分散のみ(σ調整なし)** | 5.82 | **22.18** |
+| IMU×30のみ(σ調整、実共分散なし、18番) | 1.32 | **12.06** |
+| 実共分散 + IMU×30 + yaw_sigma=0.15 | 6.17 | **22.99** |
+
+## 所見
+
+1. **実共分散単体はbaselineより明確に改善**(max 27.7→22.18mm、約20%減、
+   mean半減)。②の実共分散配信自体は無害どころか有効に働いている。
+2. **しかしIMU×30単体(12.06mm)には遠く及ばない**。
+3. **重ね合わせても加算的に効かない**: 実共分散+IMU×30の組み合わせ(22.99mm)は
+   実共分散単体(22.18mm)とほぼ同値で、IMU×30単体(12.06mm)より明確に悪い。
+   つまり実共分散を有効にした状態では、IMU側をいくら緩めてもその恩恵が
+   ほぼ消えてしまう。
+4. backendの`wall prior loosened by ICP covariance`診断ログ(x/y並進のみ監視)は
+   両runとも0件 — ただしこの診断はyaw方向の緩みを見ていないため、yaw_sigmaが
+   Censi値で押し上げられているかどうかはこのログからは判定できない
+   (yaw方向の直接ログは未実装)。
+5. 仮説: 旋回中は見える壁面の法線方向分布が偏り、平面ソルバーのyaw方向
+   情報量(Hessianのyaw対角/相関)が一時的に低下する可能性がある。実共分散は
+   これを正しく検出して「まさにIMUを信じるべきでない旋回中に」壁priorのyaw
+   σを緩めてしまい、IMU緩和策の効果を打ち消している可能性がある
+   (未確認、直接のyaw_sigma値ログが必要)。
+
+## 追加: 標準診断(wall_sigma_diag)の追加と直接確認 (2026-07-31)
+
+「ヨー方向で問題が繰り返し起きる」という指摘を受け、憶測で次の一手を決めず、
+まずbackend側のσ計算過程を毎スキャン記録する標準診断を追加した
+(`lio_localization/include/lio_localization/backend_optimizer_node.hpp`の
+`WallSigmaRecord`、`diag_wall_sigma_path`パラメータ、既定は空文字列=無効、
+`DiagRecorder`と同じ低オーバーヘッド設計)。記録項目: 対応点数(全体/X/Y)、
+縮退フラグ、Censiσ(x,y,yaw)、ICPクランプ前のヒューリスティックσ、DCS/高
+ダイナミクス適用後の最終σ。
+
+`wallsigma_diag_realcov_imu30_20260730`(実共分散+IMU×30+yaw_sigma=0.15の
+組み合わせ)で記録・解析した結果:
+
+1. **ヨー方向の「実共分散が緩めてしまう」という前セクションの仮説は誤りだった**。
+   旋回窓(t=9.5〜16.0s)の全サンプルで`censi_yaw`(0.0009〜0.0027rad)は
+   ヒューリスティック`heuristic_yaw_sigma`(0.00645rad、yaw_sigma=0.15/√541)を
+   一度も超えず、`final_yaw_sigma`はこの区間ずっとヒューリスティックのまま
+   (clampの`max(heuristic, censi)`で常にheuristic側が勝つ)。ヨーは無関係。
+2. **実際に緩んでいたのはX方向**。t=11.801〜12.151sの約0.35秒間、
+   `censi_x`(0.00195〜0.00205)がヒューリスティック`heuristic_x_sigma`
+   (≈0.00186)をわずかに上回り、`final_x_sigma`が0.00183→0.00205へ
+   約12%緩む。この直後(約1.2〜1.6秒遅れ、fixed-lag smootherの伝播遅延と
+   整合)、17番で確認したstate_estimateの誤差ピーク(t=13.40s、24.9mm)が
+   現れる。同じ区間でY方向は逆に`censi_y`が0.00101から最低0.00056まで
+   **下がり**(ヒューリスティックを一度も超えない)、Yは常にヒューリスティック
+   のまま緩んでいない。
+3. 解釈: 旋回中は見える壁面の構成が変わり、X方向の対応点由来の情報量が
+   一瞬だけ実際に低下する(これ自体は物理的に妥当な検出)。しかし実共分散は
+   これを検出して「まさにIMU予測がX方向にドリフトし始めるタイミングで」
+   壁のX補正を12%緩めてしまい、IMU×30で既に緩めてある予測がその隙に
+   さらにドリフトし、smootherが1.5秒後に気づいて戻すまでの間に大きな
+   ピークとして現れている、という筋が最も辻褄が合う(ただし因果の完全な
+   証明ではなく、タイミングの一致による強い状況証拠)。
+
+## 訂正: 上記の「実共分散とIMU緩和が排他的」という結論は検証手順のミスだった (2026-07-31)
+
+X方向クランプ(`wall_cov_max_loosen_ratio`)を追加してA/Bテストしたところ、
+クランプの有無で結果が変わらなかった(ratio=1.0でも22.98mm、無制限でも
+22.99mm)。おかしいと考えて`--imu-param`と`--backend-param`の運用を洗い直した
+結果、**`accelerometer_sigma`/`gyroscope_sigma`/`integration_sigma`は
+`imu_preintegration_node`と`backend_optimizer_node`の両方が同名パラメータを
+宣言しており、実際の③因子グラフ融合に使われるのはbackend側の値**である
+ところ、本セクション(「実共分散+IMU×30」等)の検証はすべて`--imu-param`
+(①の診断用/odom_fastにしか効かない)で渡していたため、③のIMUノイズは
+実際には一度も緩んでいなかったことが判明した(18番の元マニフェストを
+確認し、`sweep_imuinflate_30x`が`--backend-param`で渡していたことと対比して
+特定)。
+
+正しく`--backend-param`で渡して再検証(`realcov_imu30_correctparam_20260731`):
+
+| 条件 | t=15 max[mm] |
+|---|---:|
+| 18番: IMU×30のみ(旧固定共分散1e-5、backend-param) | 12.06 |
+| **本実装の実共分散 + IMU×30(backend-param、正しい配線)** | **11.85** |
+
+**実共分散とIMU緩和は排他的ではなく、正しく組み合わせれば旧結果と同等以上**。
+上記のX方向クランプ・「censi_xが旋回中に緩む」という観測自体(wallsigma_diag
+の生データ)は事実として残るが、「それがIMU緩和の効果を打ち消す」という
+因果の結論は誤りだった(IMU緩和がそもそも③に届いていなかったため、
+「打ち消す対象」自体が無かった)。`wall_cov_max_loosen_ratio`パラメータは
+実害のない安全弁として残すが、確認された不具合の修正ではなく予防的な
+安全マージンという位置づけに変更する。
+
+## 結論と次の一手(2026-07-31 訂正版)
+
+候補2(実共分散)と候補C(IMU緩和)は排他的ではなく、正しく組み合わせれば
+**11.85mm**(18番の12.06mmと同等以上)。ただし**5mm目標にはまだ届かない**。
+候補1(バイアスσ)・候補2単体の効果・候補Cの機序はいずれも実証済みで、
+組み合わせても12mm前後が下限になっている。次の選択肢:
+
+- (a) 候補3(ISAM2 relinearize_threshold)を検証する — 未着手の唯一の主要候補
+- (b) X方向の一時的な情報量低下(wallsigma_diagで観測、censi_xが旋回中の
+  0.35秒だけヒューリスティックの1.12倍まで上昇)がなぜ起きるかを②側で
+  直接確認する(実害は今のところ確認されていないが、原理的な弱点ではある)
+- (c) `wall_prior_base_yaw_sigma=0.15`+IMU×30をyamlへ正式反映し、5mm未達を
+  前提に別の切り分け軸(smoother_lag_sec等)を探す
+
+いずれも未着手。ユーザー判断待ち。
+
+## 対応状態
+
+- `lio_localization/src/laser_scan_matching_node.cpp`変更・ビルド済み(未コミット)
+- `lio_localization/include/lio_localization/backend_optimizer_node.hpp`・
+  `src/backend_optimizer_node.cpp`に`diag_wall_sigma_path`診断機能と
+  `wall_cov_max_loosen_ratio`(既定1e6=実質無制限、安全弁として保持)を追加・
+  ビルド済み(いずれも既定で無効/無変化、未コミット)
+- 実験生成物: `docs/stage4_runs/realcov_baseline_20260730/`、
+  `docs/stage4_runs/realcov_plus_imu30_20260730/`、
+  `docs/stage4_runs/wallsigma_diag_realcov_imu30_20260730/`、
+  `docs/stage4_runs/realcov_imu30_correctparam_20260731/`(いずれも未コミット)
+- yamlへのσ変更は未反映(検証時の`--backend-param`一時上書きのみ)
+- **教訓**: `accelerometer_sigma`/`gyroscope_sigma`/`integration_sigma`は
+  `imu_preintegration_node`と`backend_optimizer_node`の両方が同名パラメータを
+  持つ。③の融合結果に効かせたい場合は必ず`--backend-param`で渡すこと
+  (`--imu-param`は①の診断用/odom_fastにしか効かない)。
